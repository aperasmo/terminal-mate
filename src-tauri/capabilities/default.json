{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for TerminalMate.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}

